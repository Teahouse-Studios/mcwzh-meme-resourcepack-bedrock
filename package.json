{
    "private": true,
    "scripts": {
        "format": "prettier --write ./**/*.{js,json}",
        "test-build": "node preset_build.js"
    },
    "dependencies": {
        "glob": "^8.0.3",
        "memepack-builder": "^0.4.0"
    },
    "devDependencies": {
        "prettier": "^2.8.0"
    }
}
